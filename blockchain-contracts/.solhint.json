{
  "extends": "solhint:recommended",
  "rules": {
    "compiler-version": ["error", "^0.8.24"],
    "func-visibility": ["error", { "ignoreConstructors": true }],
    "private-vars-leading-underscore": "off",
    "reason-string": ["error", { "maxLength": 128 }],
    "ordering": "warn",
    "not-rely-on-time": "off"
  }
}
