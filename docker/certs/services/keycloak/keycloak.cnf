[req]
default_bits = 2048
prompt = no
default_md = sha256
distinguished_name = dn
req_extensions = req_ext

[dn]
C = US
ST = CA
L = SanFrancisco
O = ThaliumX
OU = development
CN = thaliumx-keycloak

[req_ext]
subjectAltName = DNS:thaliumx-keycloak,DNS:keycloak,DNS:localhost,IP:127.0.0.1
